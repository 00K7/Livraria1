<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.livraria">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Livraria"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".gps"
            android:exported="true" />
        <activity
            android:name=".salvar"
            android:exported="true" />
        <activity android:name=".Livro5Activity" />
        <activity android:name=".Livro4Activity" />
        <activity android:name=".Livro3Activity" />
        <activity android:name=".Livro2Activity" />
        <activity android:name=".Livro1Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>